<div class="container">
	<div class="row">
		<div class="col-md-3">

		   <%= render 'users/info', user: @user_you %>
           <%= render 'books/new', book: @post_book %>

        </div>
        <div class="col-md-9">
        	<h2>Books</h2>
        	  <table class="table table-hover">
        	  	<thead>
        	  		<tr>
        	  			<th></th>
        	  			<th>title</th>
                        <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
        	  			<th>opinion</th>
        	  		</tr>
        	  	</thead>
                <% @books.each do |book| %>
            	  	<tbody>
            	  		<tr>
            	  			<td>
                                <%= link_to user_path(book.user.id) do %>
            	  				  <%= attachment_image_tag book.user, :profile_image, :fill, 50, 50, format: 'jpeg', fallback: 'no_image.png' %>
                                <% end %>
            	  			</td>
            	  			<td>
            	  				<%= link_to book.title, book_path(book.id) %>
            	  			</td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
            	  			<td>
            	  				<%= book.body %>
            	  			</td>
            	  		</tr>
            	  	</tbody>
                <% end%>
        	  </table>
        </div>
    </div>
</div>