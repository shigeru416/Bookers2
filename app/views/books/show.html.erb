<div class="container">
	<div class="row">
		<div class="col-md-3">

		   <%= render 'users/info', user: @user_you %>
           <%= render 'books/new', book: @post_book %>

        </div>
        <div class="col-md-9">
        	<h2>Book detail</h2>
        	  <table class="table table-hover">
        	  	<tbody>
        	  		<tr>
        	  			<td>
                            <%= link_to user_path(@user.id) do %>
            	  				<%= attachment_image_tag @user, :profile_image, :fill, 100,100, format: 'jpeg', fallback: 'no_image.png' %><br>
                                <%= @user.name %>
                            <% end %>
        	  			</td>
        	  			<td>
        	  				<%= link_to @book.title, book_path(@book.id) %>
        	  			</td>
        	  			<td>
                            <%= @book.body %>
        	  			</td>
        	  			<td>
        	  				<%= link_to "Edit", edit_book_path(@book.id), class: "btn btn-success btn-xs" %>
        	  			</td>
                        <td>
                            <%= link_to "Destroy", book_path(@book.id), class: "btn btn-danger btn-xs", method: :delete %>
                        </td>
        	  		</tr>
        	  	</tbody>
        	  </table>
        </div>
    </div>
</div>