<h1>User info</h1>
<%= form_for(@user) do |f| %>
	<div class="form-group">
		<%= f.label :name %>
		<%= f.text_field :name, autofocus: true %>
	</div>
    <div class="form-group">image挿入</div>
	<div class="form-group">
		<%= f.label :introduction %>
		<%= f.text_area :introduction, autfocus: true %>
	</div>
	<%= f.submit "Update User", class: "btn btn-default" %>
<% end%>